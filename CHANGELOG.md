# Changelog

## v2.0.0

- Prepared a shareable, credential-free project template.
- Added `stream.env.example` for user-provided stream URLs and keys.
- Documented setup, launch, logs, and credential handling.
- Excluded local stream configuration and generated HLS output from Git and Docker build context.
